"""Server subpackage — the detection server (runs on the SOC machine).

Contains persistence (:mod:`server.db`), the ML model (:mod:`server.ml`), the
Q-learning verifier (:mod:`server.rl`), the streaming pipeline
(:mod:`server.pipeline`), and the FastAPI app (:mod:`server.api`,
:mod:`server.main`).
"""
